/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [],
        dangerouslyAllowSVG: true,
        unoptimized: true,
    }
};

export default nextConfig;
